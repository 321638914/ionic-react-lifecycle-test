import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.react.test',
  appName: 'ionic-react-test',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
